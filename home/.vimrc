set number
set relativenumber
set copyindent
set autoindent
set nocompatible
filetype off
filetype plugin indent on
syntax on
set shell=/bin/bash

" vim-tmux-navigator (from the old nix setup) is not installed — no plugin
" manager here; add vim-plug + the plugin if you want it back
